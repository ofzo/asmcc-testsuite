{
  "name": "asmcc-testsuite",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "mkdir output && node main.js && mkdir src && cp -r output/*.html src/"
  },
  "dependencies": {
    "handlebars": "^4.7.7",
    "vercel": "^31.1.0"
  }
}
